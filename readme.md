## Setup Instructions
1. Clone the repo: `git clone [url]`
2. Create venv: `python -m venv venv`
3. Install requirements: `pip install -r requirements.txt`
4. Create a `.env` file with your `DISCORD_TOKEN`.
5. Run the bot: `python main.py`